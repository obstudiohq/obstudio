# Releases

<Subtitle>Changelogs for each Obstudio release.</Subtitle>

import { ReleaseTimeline } from 'docs/src/components/ReleaseTimeline';

<ReleaseTimeline />

## Canary releases

A canary release is published for every master commit and pull request. Install one by using the corresponding pkg.pr.new URL:

```bash title="Terminal"
# Install by master commit hash
npm i https://pkg.pr.new/@obstudio/react@ad745f1

# Install by PR number
npm i https://pkg.pr.new/@obstudio/react@3713
```

Your `package.json` will then reference the pkg.pr.new URL:

```json title="package.json"
{
  "dependencies": {
    "@obstudio/react": "https://pkg.pr.new/@obstudio/react@..."
  }
}
```

Canary releases may contain breaking changes. Check the associated pull requests on GitHub for details.

## Full release notes

You can see the [full changelog on GitHub](https://github.com/obstudiohq/obstudio/blob/master/CHANGELOG.md).

export const metadata = {
  keywords: [
    'Obstudio releases',
    'Obstudio changelog',
    'Release notes beta',
    'Breaking changes Obstudio',
    'Component updates history',
    'Headless React release log',
    'Version history',
    'Changelog history',
    'Upgrade guide',
    'Migration guide',
    "What's new",
    'Release updates',
  ],
};
